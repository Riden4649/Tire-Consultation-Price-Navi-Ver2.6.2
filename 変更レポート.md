# 改修レポート

## 変更点一覧

- 黒・赤の業務ツール調から、淡いグリーンとグレージュを基調にした商談向けデザインへ刷新
- ブランド → 商品（サブブランド）→ インチ → サイズのアコーディオン検索に変更
- 選択済み条件を上部にまとめて表示
- タブレットで押しやすい大型ボタンと十分な余白を採用
- 価格表と掛率設定を折りたたみ、商談中に不要な情報を隠す構成へ変更
- 商品名を各元シートの見出しから自動推定
- Excelから取得したブランドと商品を動的に選択肢へ反映
- 検索結果に1本価格と4本価格を表示
- 「お客様へ表示」ボタンと全画面の商談モードを追加
- REGNO、POTENZA、Playz、ECOPIA、ALENZAなどにブランド別の表示テーマを適用
- 原価を画面に表示しない仕様を維持
- HTML、CSS、商品設定、Excel解析、価格計算、画面ロジックを分離

## 追加機能

- アコーディオン式の段階検索
- サブブランド自動取得
- インチ自動抽出
- 選択内容サマリー
- ブランド別商談テーマ
- お客様提示用全画面表示
- Escキー・背景タップによる商談画面終了
- Excel全シート取込
- ブランド別掛率、加算額、消費税、100円単位切上げ
- 設定のブラウザ内保存

## フォルダ構成

```text
bridgestone-price-app/
├── index.html
├── README.md
├── 変更レポート.md
├── css/
│   └── styles.css
├── data/
│   └── brand-config.js
├── js/
│   ├── app.js
│   ├── pricing.js
│   └── workbook.js
└── vendor/
    └── jszip.min.js
```

フォルダをそのままコピーし、`index.html` を開けば利用できます。

## 2026年6月24日 追加修正

- 固定 `data/catalog.js` の自動読込を廃止
- Excel読込成功時だけ検索可能に変更
- Excel解析開始時に既存の商品データを破棄
- Excel解析失敗時は空データのまま検索無効
- 現在の読込ファイル名、読込日時、シート数、商品数、データ元を表示
- 「データをクリア」ボタンを追加
- ブランドを複数選択へ変更
- ブランドに「すべて選択」「すべて解除」を追加
- 商品を複数選択へ変更
- 商品に「すべて選択」「すべて解除」を追加
- 同一サイズの複数ブランド・複数商品を比較表示
- 現在版では、最後に読込成功した商品データをlocalStorageへ保存し、次回起動時に復元します。「データをクリア」で保存データも削除します。

## 2026年6月24日 ファイル読込互換性修正

- Safari系ブラウザでExcel内部XMLの解析が止まる可能性があった処理を修正
- XML名前空間や`:scope`セレクタに依存しない解析方式へ変更
- ファイル選択欄をブラウザ標準のファイル入力へ変更
- 同じExcelファイルを続けて選び直しても再読込できるように変更
- ページへのファイルドロップ時にExcelが別画面で開かないよう抑止
- 読込失敗時に原因メッセージを画面へ表示
- Safariでファイル選択確定が取り消される問題を修正
- 透明な全面入力方式を廃止し、Safariでも確実に操作できる標準選択ボタン方式へ変更
- Excel内容を取得してから既存データを初期化する安全な読込順序へ変更

## 2026年6月24日 Excel読込機能復旧

- 正常動作していたファイル選択用label＋非表示input構成へ復元
- 正常動作していたドラッグ＆ドロップイベント処理へ復元
- Excel読込開始時に旧データを消去する処理を維持
- 読込失敗時に商品・検索条件・検索結果を空にする処理を維持
- 固定catalogやサンプルデータへのフォールバックなし
- Excel内部XMLの読込処理を正常動作版へ復元
- キャッシュ用クエリ文字列を撤去

## 2026年6月25日 サイズ・ブランド・価格計算修正

- タイヤサイズ、ロードインデックス、速度記号を1つのタイヤサイズ文字列へ統合
- 統合したタイヤサイズを選択画面、検索結果、商談画面、コピー内容で共通利用
- 初期選択ブランドをREGNO、POTENZA、Playz、FINESSA、ECOPIAの5ブランドに設定
- ブランド複数選択と選択解除による絞り込みを維持
- 価格計算を「原価 ÷ 掛率 ＋ 加算額」に変更
- 掛率へ0.9、0.85、0.8などの小数を入力可能
- 掛率が空欄、0、または不正値の場合は1.0として安全に計算
- 税込計算後に1本価格を100円単位で切り上げ、4本価格を自動計算
- 検索結果に「提案内容をコピー」を追加
- 更新前ファイルがブラウザキャッシュに残らないよう、構成ファイルへ版番号を付与

## 2026年6月25日 商談検索初期状態修正

- REGNO、POTENZA、Playz、FINESSA、ECOPIAを価格表読込直後から選択済みに変更
- 初期選択5ブランドに属する商品を、価格表読込直後からすべて選択済みに変更
- ブランドを追加選択した時、そのブランドの商品も自動ですべて選択
- ブランドの選択を外した時、そのブランドの商品だけを検索対象から除外
- 「すべて選択」で全ブランドと全商品を同時に検索対象へ追加
- タイヤサイズ内のロードインデックス・速度記号を `195/65R15 91H` 形式へ正規化
- ロードインデックスや速度記号だけの単独候補を生成しない処理を強化
- 価格表にロードインデックスがない場合は速度記号のみをサイズ末尾へ付加し、架空の値は補完しない
- ブラウザキャッシュ更新番号を `20260625-v4` へ更新

## 2026年6月25日 サイズ選択カード統合修正

- サイズ選択カードからロードインデックス・速度記号を除外
- `155/65R14 H` と `155/65R14 S` を `155/65R14` の1カードへ統合
- 統合カードの商品数を、速度記号・ロードインデックス違いを含む合計件数へ変更
- `H`、`S`、`V`、`W`、`91H`、`98V`など、基本サイズを含まない値をカード候補から除外
- 基本サイズカードを選択した検索では、該当する全規格の商品を結果へ表示
- 検索結果、商談画面、コピー内容では従来どおりフルサイズ表示を維持
- ブラウザキャッシュ更新番号を `20260625-v5` へ更新

## 2026年6月25日 ブランド初期全選択修正

- 「ブランドを選ぶ」画面の初期値を価格表内の全ブランド選択へ変更
- 価格表読込直後から、全ブランドに属する全商品も選択済み
- 「選択をクリア」後も、全ブランド・全商品を選択した初期状態へ戻る
- 個別ブランドの解除・再選択、すべて解除・すべて選択の動作を維持
- ブラウザキャッシュ更新番号を `20260625-v6` へ更新

## 2026年6月25日 工賃・総額表示追加

- 7項目の1本あたり工賃設定を追加
- 初期工賃を組替え1,100円、脱着550円、バランス550円、バルブ275円、廃タイヤ550円、窒素550円、袋50円に設定
- 工賃1本分と4本分の合計を設定画面へリアルタイム表示
- 「工賃を含める」スイッチを追加し、初期値をONに設定
- 検索結果へタイヤ1本、タイヤ4本、工賃4本分、工賃込み総額を表示
- 商談画面へ工賃4本分と工賃込み総額を追加
- コピー内容へ工賃4本分と工賃込み総額を追加
- OFF時は検索結果・商談画面・コピーから工賃と総額を非表示
- 工賃金額とON/OFF状態を既存のブラウザ設定へ保存
- 既存の掛率・加算額・消費税率設定を維持したまま新設定を追加
- 原価を検索結果・商談画面・コピーへ表示しない仕様を維持
- ブラウザキャッシュ更新番号を `20260625-v7` へ更新
