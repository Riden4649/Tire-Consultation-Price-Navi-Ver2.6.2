# 冬価格表対応 変更レポート

## 対応内容

- 2025年冬卸価格表に対応しました。
- 読み込んだExcelのシート名・シート内レイアウトから、夏用価格表 / 冬用価格表を自動判定するようにしました。
- 夏用価格表の既存解析ルートは維持し、互換性を崩さない構成にしています。
- 冬用シート `PYR①` / `PYR②` / `V` / `ｱｲｽﾊﾟ` の横並び価格表レイアウトを解析できるようにしました。
- 読み込み済み価格データをブラウザ内に保存し、次に別の価格表を読み込むまで復元できるようにしました。
- 「データをクリア」実行時は保存データも削除され、未読込状態へ戻ります。

## 冬価格表の解析方針

### PYR① / PYR②

- `ﾌﾞﾗﾝﾄﾞ` / `ﾊﾟﾀﾝ` 行を検出
- `BLIZZAK` をブランドとして取得
- `WZ-1`、`VRX3`、`WZ-1(SUV)`、`VRX3(SUV)`、`DM-V3` などを商品として取得
- サイズ列の右側に並ぶ商品コード・卸価格を取得

### V

- `ｻｲｽﾞ` 行を検出
- `LI` / `PR` をタイヤサイズの一部として扱い、検索結果ではフルサイズ表示に対応
- `VL10A`、`VL10`、`W300`、`W989`、`W979`、`M807`、`M812Ⅱ` などを取得
- 見出し行と明細行が同じ行に混在する箇所も考慮

### ｱｲｽﾊﾟ

- `ICEPARTNER2` を商品として取得
- 商品コード・卸価格を取得
- ブランドは `ICEPARTNER` として表示

## 追加ブランド

- BLIZZAK
- ICEPARTNER
- オールシーズン

## 保存機能

- 読込成功後、以下をブラウザ内に保存します。
  - ファイル名
  - 読込日時
  - シート一覧
  - 商品データ
- 次回起動時に保存データを自動復元します。
- 新しいExcelを読み込むと、古い保存データは破棄され、新しいデータに置き換わります。
- 読込失敗時は古いデータを表示しません。

## 確認結果

- `2025冬卸価格.xlsm`
  - Excel構造確認OK
  - ワークシートXML数：4
  - 対象シート：`PYR①` / `PYR②` / `V` / `ｱｲｽﾊﾟ`
- `販売価格.xlsm`
  - Excel構造確認OK
  - ワークシートXML数：14
- JavaScript構文確認OK
  - `js/workbook.js`
  - `js/app.js`
  - `data/brand-config.js`
  - `service-worker.js`

## 注意

- UIデザイン・検索手順は変更していません。
- 原価は引き続き画面に表示しません。
- PWAキャッシュ更新のため、アプリ内ファイル参照バージョンを `20260626-v9` に更新しています。
